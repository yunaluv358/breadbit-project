CREATE TABLE bread
(
    `bread_id`           INT             NOT NULL    AUTO_INCREMENT,
    `bread_name`         VARCHAR(45)     NULL,
    `bread_image`  VARCHAR(200)    NULL,
    `bread_price`        VARCHAR(45)     NULL,
    `bread_description`  VARCHAR(200)    NULL,
    PRIMARY KEY (bread_id)
);